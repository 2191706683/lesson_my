{
  "name": "pacong",
  "version": "1.0.0",
  "description": "- top250 豆瓣 移动时代 被抛弃了\r     MVVM 是现代vue，react 开发思想\r         vue index.html #app 挂载点 相应的更快，\r         拿到之后，通过前后端分离，使用axios发送xhr请求\r         api 后端返回的是json 而不是html\r         reactive 显示到页面\r     MVC  传统的后端驱动web架构\r         M 数据 -> V 渲染 -> res返回给前端的是完整html",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "request-promise": "^4.2.6"
  }
}
